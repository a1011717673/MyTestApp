<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.liujinxing.mytestapp">

    <application
        android:name=".constant.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".EventBusActivity"/>
        <activity android:name=".EventBusTestActivity"/>
        <activity android:name=".router.RouterActivity"/>
        <activity android:name=".router.Activity1"/>
        <activity android:name=".router.RActivity2"/>
        <activity android:name=".router.CustomGroupActivity"/>
        <activity android:name=".router.WebActivity"/>
        <activity android:name=".ListViewDelDemoActivity"/>
        <activity android:name=".listview.RefreshListActivity"/>
    </application>

</manifest>